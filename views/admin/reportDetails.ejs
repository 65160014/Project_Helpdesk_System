<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style_dashboard.css"> <!-- Adjust path as necessary -->
    <title>Report Details</title>
</head>
<body>
    <div class="container">
        <div class="sidebar">
            <div>
                <h1>HELPDESK</h1>
                <h3>General</h3>
                <ul>
                    <li class="active"><a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
                    <li><a href="/admin/tickets"><i class="fas fa-ticket-alt"></i> Tickets</a></li>
                    <!-- <li><a href="/admin/search"><i class="fas fa-search"></i> Search</a></li> -->
                </ul>
                <hr>
                <h3>Administration</h3>
                <ul>
                    <li><a href="/admin/status"><i class="fas fa-tasks"></i> Status</a></li>
                    <li><a href="/admin/queue"><i class="fas fa-user-clock"></i> Queue</a></li>
                    <li><a href="/admin/userList"><i class="fas fa-users"></i> Users</a></li>
                </ul>
            </div>
            <div class="profile">
                <a href="/profile"><i class="fas fa-user-circle"></i></a>
            </div>
        </div>

        <div class="content">
            <div class="header">
                <h1>Report Details</h1>
                <a href="/admin/dashboard" class="new-report">Back to Dashboard</a>
            </div>
            <% if (report) { %>
                <form class="report-form" action="/admin/report/edit/<%= report.report_id %>" method="POST">
                    <div class="form-group">
                        <h3>Title</h3>
                        <input type="text" id="title" name="title" class="form-input" value="<%= report.title %>" required>
                    </div>

                    <div class="form-group">
                        <h3>Content</h3>
                        <textarea id="content" name="content" class="form-textarea" required><%= report.content %></textarea>
                    </div>

                    <!-- Status Dropdown -->
                    <div class="form-group">
                        <h3>Status</h3>
                        <select name="status" class="form-select" required>
                            <option value="show" <%= report.status === 'show' ? 'selected' : '' %>>Show</option>
                            <option value="hide" <%= report.status === 'hide' ? 'selected' : '' %>>Hide</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <button type="submit" class="submit-button">Update Report</button>
                    </div>
                </form>
            <% } else { %>
                <p>Report not found.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
